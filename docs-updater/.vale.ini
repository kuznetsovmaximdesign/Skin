# Конфигурация Vale. Работает офлайн: правила лежат рядом, в styles/.
# Vale необязателен — если бинарника нет, сервис использует встроенные правила.
StylesPath = styles
MinAlertLevel = suggestion
Vocab = Corp

[*.md]
BasedOnStyles = Corp
